import type { PlatformRule } from '@/types';
import { DEFAULT_ANGLES, DEFAULT_NAMING_TEMPLATE } from '@/types/constants';
import { generateId } from '@/utils/formatters';

export const DEFAULT_PLATFORM_RULES: PlatformRule[] = [
  {
    id: generateId(),
    name: '淘宝',
    logo: '🛒',
    enabled: true,
    namingTemplate: DEFAULT_NAMING_TEMPLATE,
    imageRequirements: [
      {
        type: 'main',
        minWidth: 800,
        maxWidth: 5000,
        minHeight: 800,
        maxHeight: 5000,
        maxFileSize: 3 * 1024 * 1024,
        aspectRatio: '1:1',
        requireWhiteBackground: true,
      },
      {
        type: 'detail',
        minWidth: 750,
        maxWidth: 1500,
        minHeight: 1000,
        maxHeight: 20000,
        maxFileSize: 3 * 1024 * 1024,
        aspectRatio: 'any',
        requireWhiteBackground: false,
      },
      {
        type: 'scene',
        minWidth: 800,
        maxWidth: 5000,
        minHeight: 800,
        maxHeight: 5000,
        maxFileSize: 3 * 1024 * 1024,
        aspectRatio: 'any',
        requireWhiteBackground: false,
      },
    ],
    settings: {
      whiteBackgroundThreshold: 0.95,
      duplicateThreshold: 5,
      requiredAngles: DEFAULT_ANGLES.slice(0, 5),
    },
    createdAt: Date.now(),
    updatedAt: Date.now(),
  },
  {
    id: generateId(),
    name: '京东',
    logo: '📦',
    enabled: true,
    namingTemplate: '{商品编码}_{类型}_{序号}',
    imageRequirements: [
      {
        type: 'main',
        minWidth: 800,
        maxWidth: 2000,
        minHeight: 800,
        maxHeight: 2000,
        maxFileSize: 2 * 1024 * 1024,
        aspectRatio: '1:1',
        requireWhiteBackground: true,
      },
      {
        type: 'detail',
        minWidth: 750,
        maxWidth: 990,
        minHeight: 500,
        maxHeight: 20000,
        maxFileSize: 2 * 1024 * 1024,
        aspectRatio: 'any',
        requireWhiteBackground: false,
      },
      {
        type: 'scene',
        minWidth: 800,
        maxWidth: 2000,
        minHeight: 800,
        maxHeight: 2000,
        maxFileSize: 2 * 1024 * 1024,
        aspectRatio: 'any',
        requireWhiteBackground: false,
      },
    ],
    settings: {
      whiteBackgroundThreshold: 0.95,
      duplicateThreshold: 5,
      requiredAngles: DEFAULT_ANGLES.slice(0, 4),
    },
    createdAt: Date.now(),
    updatedAt: Date.now(),
  },
  {
    id: generateId(),
    name: '拼多多',
    logo: '🍎',
    enabled: true,
    namingTemplate: '{商品编码}_{类型}_{序号}_{角度}',
    imageRequirements: [
      {
        type: 'main',
        minWidth: 500,
        maxWidth: 3000,
        minHeight: 500,
        maxHeight: 3000,
        maxFileSize: 1 * 1024 * 1024,
        aspectRatio: '1:1',
        requireWhiteBackground: true,
      },
      {
        type: 'detail',
        minWidth: 480,
        maxWidth: 1500,
        minHeight: 480,
        maxHeight: 1500,
        maxFileSize: 1 * 1024 * 1024,
        aspectRatio: 'any',
        requireWhiteBackground: false,
      },
      {
        type: 'scene',
        minWidth: 500,
        maxWidth: 3000,
        minHeight: 500,
        maxHeight: 3000,
        maxFileSize: 1 * 1024 * 1024,
        aspectRatio: 'any',
        requireWhiteBackground: false,
      },
    ],
    settings: {
      whiteBackgroundThreshold: 0.9,
      duplicateThreshold: 5,
      requiredAngles: DEFAULT_ANGLES.slice(0, 3),
    },
    createdAt: Date.now(),
    updatedAt: Date.now(),
  },
  {
    id: generateId(),
    name: '抖音小店',
    logo: '🎵',
    enabled: false,
    namingTemplate: '{平台}_{商品编码}_{类型}_{序号}',
    imageRequirements: [
      {
        type: 'main',
        minWidth: 600,
        maxWidth: 2000,
        minHeight: 600,
        maxHeight: 2000,
        maxFileSize: 2 * 1024 * 1024,
        aspectRatio: '1:1',
        requireWhiteBackground: true,
      },
      {
        type: 'detail',
        minWidth: 600,
        maxWidth: 2000,
        minHeight: 600,
        maxHeight: 5000,
        maxFileSize: 2 * 1024 * 1024,
        aspectRatio: 'any',
        requireWhiteBackground: false,
      },
      {
        type: 'scene',
        minWidth: 600,
        maxWidth: 2000,
        minHeight: 600,
        maxHeight: 2000,
        maxFileSize: 2 * 1024 * 1024,
        aspectRatio: 'any',
        requireWhiteBackground: false,
      },
    ],
    settings: {
      whiteBackgroundThreshold: 0.95,
      duplicateThreshold: 5,
      requiredAngles: DEFAULT_ANGLES.slice(0, 5),
    },
    createdAt: Date.now(),
    updatedAt: Date.now(),
  },
];

export const MOCK_PROCESS_RECORDS = [
  {
    id: generateId(),
    platformName: '淘宝',
    folderPath: 'D:/电商素材/2024夏季新品',
    totalImages: 156,
    issueCount: 12,
    status: 'completed' as const,
    startTime: Date.now() - 86400000 * 2,
    endTime: Date.now() - 86400000 * 2 + 1800000,
  },
  {
    id: generateId(),
    platformName: '京东',
    folderPath: 'D:/电商素材/618活动款',
    totalImages: 89,
    issueCount: 5,
    status: 'completed' as const,
    startTime: Date.now() - 86400000,
    endTime: Date.now() - 86400000 + 900000,
  },
  {
    id: generateId(),
    platformName: '拼多多',
    folderPath: 'D:/电商素材/清仓款',
    totalImages: 234,
    issueCount: 28,
    status: 'completed' as const,
    startTime: Date.now() - 3600000 * 5,
    endTime: Date.now() - 3600000 * 5 + 3600000,
  },
];
